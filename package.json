{
  "name": "mokki-canasta",
  "version": "0.1.0",
  "description": "Annelin Canasta — 1-4 pelaajaa, Annelin saannoilla",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "test": "node --test",
    "itest": "node scripts/itest.mjs",
    "start": "node build.mjs && node server.mjs"
  },
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.28.1"
  },
  "dependencies": {
    "qrcode-generator": "^2.0.4"
  }
}
